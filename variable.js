console.log("Hello World")
    // js syntax
    // variabkes in JS - containers for values let(reassigned) 
    // const (cannot change)
    let x = 8
    x = 90
    let y = 5
    y = 80
    console. log(x, y)
    const a =10
    //  a = 80; error (constant var cannot be reassigned)

   console.log(a);
   //RULES AND CONVENTIONS IN DECLARING VARIABLES
   // 1. variables must start with letters or _ cannot start with numbers
   // 2. no special characters or spaces in between
   // const firstName = 'Ade'
   // 3. variables are case sensitive my Name MYNAME
   const b = 0
   const B = 90
   // 4. reserved key words let const if, while for
   // const let = 60
   // const const = 9
   // logical variable names singular
   const w =67
   const weight = 67
   // compound names account number my age (camelCasing)
   const accountNumber = 877076
   const myAge = 78
    